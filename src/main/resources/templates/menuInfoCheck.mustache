{{>layout/header}}

{{>layout/sidebar}}

        <div class="MainArea MainAreaFont">
            <label class="MenuTitle-W">정제한 방송 정보 확인 및 작업</label>
            <br />
            <div class="row">
                <div class="col-md-6">
                    정제한 방송 정보를 확인함. 수정 및 삭제가 가능하고, 여러개 한번에 가능함.
                </div>
                <div class="col-md-3">
                    <button id="btn-menu-info-check-update" class="btn btn-primary">정보 수정</button>
                </div>
                <div class="col-md-3">
                    <button id="btn-menu-info-check-delete" class="btn btn-danger">정보 삭제</button>
                </div>
            </div>
            <hr />
            <div>
                {{#posts}}
                    <form>
                        <div class="input-group">
                            <label class="col-md-4">방송명 (요약)</label>
                            <input id="broadcastTitle{{id}}" type="text" class="form-control" placeholder="최대한 간단하게. ex)아쿠아 생방송" value="{{title}}"/>
                        </div>
                        <div class="input-group">
                            <label class="col-md-4">방송 정보 내용</label>
                            <textarea class="form-control" id="broadcastContext{{id}}">{{context}}</textarea>
                        </div>
                        <div class="input-group">
                            <label class="col-md-4">방송 정보 태그 (카테고리)</label>
                            <input id="broadcastTag{{id}}" type="text" class="form-control" placeholder="3개까지 기억됩니다. 여러개일 경우 , 로 구분해주세요." value="{{tag}}"/>
                        </div>
                        <div class="input-group">
                            <label class="col-md-4">방송 정보 일시</label>
                            <input id="broadcastDate{{id}}" type="datetime-local" class="form-control" placeholder="2020-12-31" value="{{broadcastDate}}"/>
                        </div>
                        <div class="input-group">
                            <label class="col-md-4">출처</label>
                            <a href="{{source}}" target="_blank"
                               class="btn btn-success active col-md-1" role="button">출처</a>
                            <label class="col-md-3">작업 하실 거면 체크</label>
                            <input class="form-control" type="checkbox" id="check" value="{{id}}"/>
                            <input type="hidden" value="{{source}}" id="url{{id}}" />
                        </div>
                    </form>
                    <hr />
                {{/posts}}
            </div>
        </div>
    </div>
</div>

{{>layout/footer}}