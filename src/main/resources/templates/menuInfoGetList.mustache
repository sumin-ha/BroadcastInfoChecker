{{>layout/header}}


<div class="col-md-12">
    <h1>Test!!!</h1>
    <div class="row">
        <div class="col-md-4">
            <div class="row">
                <button id="link-menuInfoRegister" class="btn btn-primary">알리미 추가 및 수정</button>
            </div>
            <div class="row">
                <button id="link-menuInfoGetList" class="btn btn-primary">방송 정보 취득</button>
            </div>
            <div class="row">
                <button id="link-menuInfoCheck" class="btn btn-primary">등록 내용 확인</button>
            </div>
            <div class="row">
                <button id="link-menuInfoManualRegister" class="btn btn-primary">정보 수동 등록</button>
            </div>
        </div>
        <div class="col-md-8">
            <h2>추출한 방송 정보 정제</h2>
            <br />
            <div class="row">
                <div class="col-md-6">
                    추출한 방송 정보를 등록.
                </div>
                <div class="col-md-6">
                    <button id="btn-menu-info-get-List" class="btn btn-primary">정보 등록</button>
                </div>
            </div>
            <hr />
            <div>
            {{#posts}}
                <form>
                    <div class="input-group">
                        <label class="col-md-4">방송명 (요약)</label>
                        <input id="broadcastTitle{{source}}" type="text" class="form-control" placeholder="최대한 간단하게. ex)아쿠아 생방송"/>
                    </div>
                    <div class="input-group">
                        <label class="col-md-4">방송 정보 내용</label>
                        <textarea class="form-control" id="broadcastContext{{source}}">{{context}}</textarea>
                    </div>
                    <div class="input-group">
                        <label class="col-md-4">방송 정보 태그 (카테고리)</label>
                        <input id="broadcastTag{{source}}" type="text" class="form-control" placeholder="3개까지 기억됩니다. 여러개일 경우 , 로 구분해주세요."/>
                    </div>
                    <div class="input-group">
                        <label class="col-md-4">방송 정보 일시</label>
                        <input id="broadcastDate{{source}}" type="datetime-local" class="form-control" placeholder="2020-12-31"/>
                    </div>
                    <div class="row">
                        <div class="input-group">
                            <label class="col-md-4">출처</label>
                            <a href="https://twitter.com/{{tweetAccount}}/status/{{source}}" target="_blank"
                               class="btn btn-success active col-md-1" role="button">출처</a>
                            <label class="col-md-3">등록 하실거라면 체크</label>
                            <input class="form-control" type="checkbox" id="check" value="{{source}}"/>
                            <input type="hidden" value="{{tweetAccount}}" id="account{{source}}" />
                        </div>
                    </div>
                </form>
                <hr />
            {{/posts}}
            </div>
        </div>
    </div>
</div>

{{>layout/footer}}